---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---
<Layout title="Insights">
  <section>
    <div class="section-head">
      <div class="kicker">Insights</div>
      <h2>Aus der Praxis, für die Praxis.</h2>
    </div>
    <div class="blog-grid">
      {posts.map((post) => (
        <article class="blog-card">
          <div class="blog-date">{post.data.date.toLocaleDateString('de-DE', { year: 'numeric', month: 'long' })}</div>
          <div class="blog-title">{post.data.title}</div>
          <div class="blog-excerpt">{post.data.excerpt}</div>
          <a class="blog-more" href={`${import.meta.env.BASE_URL.replace(/\/$/, '')}/blog/${post.slug}/`}>Weiterlesen →</a>
        </article>
      ))}
    </div>
  </section>
</Layout>
